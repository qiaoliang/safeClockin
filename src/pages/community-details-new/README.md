# Community Details New 页面

## 概述

这是社区详情页面的新版本，采用组件化架构重构，作为"先增加再删除"重构策略的第一步。

## 页面配置

### 路由路径
- **新页面**: `pages/community-details-new/community-details-new`
- **旧页面**: `pages/community-detail/community-detail` (保持原样)

### 调用方式

```javascript
// 跳转到新页面
uni.navigateTo({
  url: '/pages/community-details-new/community-details-new?id=社区ID'
})

// 跳转到旧页面（对比用）
uni.navigateTo({
  url: '/pages/community-detail/community-detail?communityId=社区ID'
})
```

## 组件结构

```
community-details-new/
├── community-details-new.vue          # 主页面
├── components/
│   ├── CommunityDetailHeader.vue      # 顶部导航组件
│   ├── CommunityInfoCard.vue          # 社区信息卡片组件
│   ├── CommunityTabBar.vue            # Tab切换栏组件
│   ├── tabs/
│   │   ├── CommunityStaffTab.vue      # 专员管理Tab
│   │   ├── CommunityUsersTab.vue      # 用户管理Tab
│   │   ├── CommunityRulesTab.vue      # 规则管理Tab (待实现)
│   │   ├── CommunityAssignTab.vue     # 分配管理Tab (待实现)
│   │   └── CommunitySupportTab.vue    # 应援管理Tab (待实现)
│   └── modals/
│       ├── CommunitySettingsModal.vue # 设置模态框
│       ├── CommunityAddStaffModal.vue # 添加专员模态框 (待实现)
│       └── CommunityAddUserModal.vue  # 添加用户模态框 (待实现)
└── README.md                          # 本文档
```

## 特性

### ✅ 已完成
1. **组件化架构** - 每个功能区域都是独立组件
2. **样式一致性** - 使用 `uni.scss` 样式系统
3. **设计还原** - 参考 HTML 设计文件的风格
4. **功能集成** - 集成现有组件的核心功能
5. **API 集成** - 使用项目现有的 API 调用模式
6. **权限检查** - 集成用户权限验证逻辑
7. **错误处理** - 完整的加载状态和错误处理

### 🔄 待完善
1. 剩余 Tab 组件的实现
2. 更多模态框的实现
3. 实际 API 连接的测试
4. 单元测试编写

## 测试页面

提供了一个测试页面用于验证新页面的调用：

```javascript
// 访问测试页面
uni.navigateTo({
  url: '/pages/test-community-details-new/test-community-details-new'
})
```

测试页面包含：
- 跳转到新页面的测试按钮
- 跳转到旧页面的对比按钮
- 新页面特性说明
- 使用说明

## 开发规范

### 样式规范
- 所有样式必须使用 `uni.scss` 中的变量和混入
- 遵循项目的设计系统
- 使用 rpx 单位确保多端适配

### 组件规范
- 使用 Vue 3 Composition API
- 明确的 Props 和 Emits 定义
- 组件职责单一化
- 良好的错误边界处理

### API 集成
- 使用 `@/api/community.js` 中的 API 函数
- 统一的错误处理模式
- 数据解码和验证

## 重构策略

### 第一阶段：增加 ✅
- 创建新的组件化页面
- 保持原有页面不变
- 并行测试新旧页面

### 第二阶段：替换
- 逐步将业务逻辑迁移到新页面
- 更新相关页面的跳转链接
- 全面测试新页面功能

### 第三阶段：删除
- 确认新页面稳定运行
- 删除旧页面文件
- 清理相关引用

## 注意事项

1. **并行存在** - 新旧页面目前并行存在，便于对比测试
2. **平滑过渡** - 采用渐进式重构，确保业务连续性
3. **向后兼容** - 保持与现有系统的兼容性
4. **团队协作** - 组件化架构便于团队协作开发

## 快速开始

1. 运行测试页面验证配置：
   ```bash
   # 在 uni-app 开发工具中运行
   ```

2. 测试新页面跳转：
   ```javascript
   uni.navigateTo({
     url: '/pages/community-details-new/community-details-new?id=1'
   })
   ```

3. 对比新旧页面：
   ```javascript
   // 新页面
   uni.navigateTo({
     url: '/pages/community-details-new/community-details-new?id=1'
   })
   
   // 旧页面
   uni.navigateTo({
     url: '/pages/community-detail/community-detail?communityId=1'
   })
   ```

## 问题排查

### 页面无法跳转
1. 检查 `pages.json` 配置是否正确
2. 确认页面路径拼写无误
3. 检查路由参数格式

### 样式不生效
1. 确认已导入 `uni.scss`
2. 检查样式变量名称
3. 验证混入函数调用

### API 调用失败
1. 检查网络连接
2. 验证 API 函数参数
3. 查看控制台错误信息

## 贡献指南

1. 遵循现有的代码风格
2. 为新功能添加测试
3. 更新相关文档
4. 保持组件接口稳定